<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.co.ngcapp.ngc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.WalkThroughOrgActivity">


            <!-- <activity android:name=".activity.MainHandlingActivity"> -->


            <!-- <activity -->
            <!-- android:name=".activity.MainMenuActivity"> -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.ContactDetailsActivity" />
        <!-- <activity -->
        <!-- android:name=".activity.TabLayoutActivity" -->
        <!-- android:theme="@style/CustomTabLayoutStyle" /> -->
        <activity
            android:name=".activity.GroupDetailsActivity"
            android:theme="@style/BaseTheme" />
        <activity android:name=".activity.CreateNewContactActivity" />
        <activity android:name=".activity.MainMenuActivity" />
        <activity android:name=".activity.CreateNewGroupActivity" />
        <!-- <activity android:name=".activity.LoginActivity"></activity> -->
        <activity android:name=".activity.LoginActivity"></activity>
        <activity android:name=".activity.MainHandlingActivity"> </activity>

    </application>

</manifest>